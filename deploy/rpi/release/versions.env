# Pinned sidecar / helper versions.
# Shared by Docker buildx (Mac/CI) and the break-glass on-Pi compiler.
# Keep in sync with the values that shipped P49 via deploy/rpi/install.sh.

NQPTP_VERSION=1.2.4
SHAIRPORT_VERSION=4.3.6
PIXLET_VERSION=0.34.0
NODE_MAJOR=20
